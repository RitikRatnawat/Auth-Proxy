apiVersion: v1
kind: Service
metadata:
  name: {{ include "Jupyter-Authentication-Proxy.fullname" . }}
  labels:
    {{- include "Jupyter-Authentication-Proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Jupyter-Authentication-Proxy.selectorLabels" . | nindent 4 }}
